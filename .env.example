# Copy this file to `.env` and fill in real values. `.env` is gitignored.
#
# Mistral API key — used for STT (Voxtral), the LLM brain, and TTS.
# Get one at https://console.mistral.ai/
MISTRAL_API_KEY=
